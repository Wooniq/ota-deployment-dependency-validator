package com.ota.control.repository;

import com.ota.control.domain.UpdatePackage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UpdatePackageRepository extends JpaRepository<UpdatePackage, String> {
}
